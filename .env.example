# Copy to .env and fill in your values for local dev builds
# .env is gitignored — your creds stay local
#
# Usage:
#   make run              — dev build with .env
#   make build-macos      — release build with .env.release (no creds)

GIT_REPO_URL=https://github.com/your-username/your-journal-repo.git
GIT_PAT=ghp_your_personal_access_token_here
